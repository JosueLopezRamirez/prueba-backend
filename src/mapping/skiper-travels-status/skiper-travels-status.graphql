input SkiperTravelsStatusInput {
    id: Int
    name: String!
    indicator: String!
    prevstatus: Int!
    errorstatusprev: String!
}

type SkiperTravelsStatusDto {
    id: Int
    name: String
    indicator: String
    prevstatus: Int
    errorstatusprev: String
}

type Query {
    getAllSkiperTravelStatus: [SkiperTravelsStatusDto!]
}