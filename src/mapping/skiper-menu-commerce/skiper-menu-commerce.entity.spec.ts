import { SkiperMenuCommerce.Entity } from './skiper-menu-commerce.entity';

describe('SkiperMenuCommerce.Entity', () => {
  it('should be defined', () => {
    expect(new SkiperMenuCommerce.Entity()).toBeDefined();
  });
});
